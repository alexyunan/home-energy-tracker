# Getting Started

## How to start
1. Run the docker containers.

2. Run the following command in the root directory of the project:
   ```
   docker compose up -d
   ```

3. to stop the containers, run:
   ```
   docker compose  down
   ```
   
You might have to delete pre-existing volumes if you are encountering issues with the database.